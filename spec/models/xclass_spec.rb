require 'rails_helper'

RSpec.describe Xclass, type: :model do
  #it 'validates presence of Name' do
  #  xclass = Xclass.new(name: 'Name')
  #	 expect(xclass.valid?).to be_falsey
  #  expect(xclass).to_not be_valid
  #  expect(Xclass.new(name: 'Name')).to_not be_valid
  #end
  
  ##should matchers
  #it { should validate_presence_of :name }

  pending "add some examples to valid?"	
end