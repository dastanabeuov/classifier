require 'rails_helper'

RSpec.describe Xroot, type: :model do
  #it 'validates presence of Name' do
  #  xroot = Xroot.new(name: 'Name')
  #	 expect(xroot.valid?).to be_falsey
  #  expect(xroot).to_not be_valid
  #  expect(Xroot.new(name: 'Name')).to_not be_valid
  #end
  
  ##should matchers
  #it { should validate_presence_of :name }

  pending "add some examples to valid?"	
end