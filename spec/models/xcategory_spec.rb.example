require 'rails_helper'

RSpec.describe Xcategory, type: :model do
  #it 'validates presence of Name' do
  #  xcategory = Xcategory.new(name: 'Name')
  #	 expect(xcategory.valid?).to be_falsey
  #  expect(xcategory).to_not be_valid
  #  expect(Xcategory.new(name: 'Name')).to_not be_valid
  #end
  
  ##should matchers
  #it { should validate_presence_of :name }

  pending "add some examples to valid?"	
end