<ul classzz="items">
  <% xclasses.each do |page, children| %>
    <li>
      <%= link_to_unless_current page.name, page %>
      <%= render 'submenu_pages', xclasses: children if children.present? %>
    </li>
  <% end %>
</ul>