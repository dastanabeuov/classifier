<div class="edit container">
  <div class="name-container name-pale-blue name-leftbar">
    <h1>Editing Category</h1>
  </div>

  <%= render 'form' %>

  <div class="btn-group">
    <%= link_to 'Show', xroot_xcategory_path(@xcategory.xroot, @xcategory), class: 'btn btn-sm btn-outline-secondary' %>
    <%= link_to 'Back', xroot_path(@xcategory.xroot), class: 'btn btn-sm btn-outline-secondary' %>
  </div>
</div>